#ifndef TRAFFICSIGNRECOGNITION_HPP
#define TRAFFICSIGNRECOGNITION_HPP

// This file was generated by the ScoreGen C++ template for runnable skeleton headers

#include <system_error>
#include <string>
#include <score/base/Memory.hpp>
#include <cstdint>
#include <score/comm/ShmSampleBuffer.hpp>
#include <score/middleware/Runnable.hpp>
#include <queue.hpp>

#include <runnable_interfaces.hpp>  // Common interface header for Runnable communication

struct TrafficSignRecognitionHiddenState {
};

class TrafficSignRecognition final : public score::middleware::Runnable<TrafficSignRecognitionInputs,
                                                                            TrafficSignRecognitionOutputs,
                                                                            TrafficSignRecognitionInternalState,
                                                                            TrafficSignRecognitionParameters> {
  public:
    std::error onInit(const TrafficSignRecognitionParameters& parameters,
                            TrafficSignRecognitionInternalState& internal_state) noexcept final;
    std::error onUpdate(const std::chrono::time_point& time,
                              const TrafficSignRecognitionParameters& parameters,
                              const TrafficSignRecognitionInputs& inputs,
                              TrafficSignRecognitionInternalState& internal_state,
                              TrafficSignRecognitionOutputs& outputs) noexcept final;
    std::error onShutdown(const TrafficSignRecognitionParameters& parameters,
                                TrafficSignRecognitionInternalState& internal_state) noexcept final;
    std::error onReset(const TrafficSignRecognitionParameters& parameters,
                             TrafficSignRecognitionInternalState& internal_state) noexcept;
  private:
    TrafficSignRecognitionHiddenState hidden_state_;
};

#endif  // TRAFFICSIGNRECOGNITION_HPP
