#ifndef SYSTEMLOGGER_HPP
#define SYSTEMLOGGER_HPP

// This file was generated by the ScoreGen C++ template for runnable skeleton headers

#include <system_error>
#include <string>
#include <score/base/Memory.hpp>
#include <cstdint>
#include <score/comm/ShmSampleBuffer.hpp>
#include <score/middleware/Runnable.hpp>
#include <queue.hpp>

#include <runnable_interfaces.hpp>  // Common interface header for Runnable communication

struct SystemLoggerHiddenState {
};

class SystemLogger final : public score::middleware::Runnable<SystemLoggerInputs,
                                                                            SystemLoggerOutputs,
                                                                            SystemLoggerInternalState,
                                                                            SystemLoggerParameters> {
  public:
    std::error onInit(const SystemLoggerParameters& parameters,
                            SystemLoggerInternalState& internal_state) noexcept final;
    std::error onUpdate(const std::chrono::time_point& time,
                              const SystemLoggerParameters& parameters,
                              const SystemLoggerInputs& inputs,
                              SystemLoggerInternalState& internal_state,
                              SystemLoggerOutputs& outputs) noexcept final;
    std::error onShutdown(const SystemLoggerParameters& parameters,
                                SystemLoggerInternalState& internal_state) noexcept final;
    std::error onReset(const SystemLoggerParameters& parameters,
                             SystemLoggerInternalState& internal_state) noexcept;
  private:
    SystemLoggerHiddenState hidden_state_;
};

#endif  // SYSTEMLOGGER_HPP
