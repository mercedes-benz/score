// This file was generated by the ScoreGen C++ template for runnable implementation skeletons

#include "runnable.hpp"  // Needs to be included first

#include <system_error>
#include <score/log/Logger.hpp>

static constexpr std::string_view kLogPrefix{ "SystemLogger" };

std::error SystemLogger::onInit(const SystemLoggerParameters& parameters,
                                                      SystemLoggerInternalState& internal_state) noexcept {
    return std::error{std::errorCode::kSuccess};
}

std::error SystemLogger::onUpdate(const std::chrono::time_point& time,
                                                        const SystemLoggerParameters& parameters,
                                                        const SystemLoggerInputs& inputs,
                                                        SystemLoggerInternalState& internal_state,
                                                        SystemLoggerOutputs& outputs) noexcept {
    if (!inputs.camera_data_log_input_queue.empty()) {
        const adas::perception::CameraFrame& camera_data_log = *inputs.camera_data_log_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }
    if (!inputs.detection_log_input_queue.empty()) {
        const adas::perception::ObjectDetections& detection_log = *inputs.detection_log_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }
    if (!inputs.brake_command_log_input_queue.empty()) {
        const adas::perception::BrakeCommand& brake_command_log = *inputs.brake_command_log_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }
    if (!inputs.traffic_sign_log_input_queue.empty()) {
        const adas::perception::TrafficSignCommand& traffic_sign_log = *inputs.traffic_sign_log_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }

    std::error output_result{std::errorCode::kSuccess};
    // The below output access examples are for reference only
    output_result = outputs.log_entries_output_queue.acquire().with_value([&parameters](auto& output_) {
        ; // TODO(end-user): Write something to the output, e.g. output_->my_field = 0;
        score::base::ignore = parameters;
        score::base::ignore = output_;
    }).error_or(std::error{std::errorCode::kSuccess});

    return output_result;
}

std::error SystemLogger::onReset(const SystemLoggerParameters& parameters,
                                                       SystemLoggerInternalState& internal_state) noexcept {
    return std::error{std::errorCode::kSuccess};
}

std::error SystemLogger::onShutdown(const SystemLoggerParameters& parameters,
                                                          SystemLoggerInternalState& internal_state) noexcept {
    return std::error{std::errorCode::kSuccess};
}
