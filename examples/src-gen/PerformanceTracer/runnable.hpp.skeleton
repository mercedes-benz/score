#ifndef PERFORMANCETRACER_HPP
#define PERFORMANCETRACER_HPP

// This file was generated by the ScoreGen C++ template for runnable skeleton headers

#include <system_error>
#include <string>
#include <score/base/Memory.hpp>
#include <cstdint>
#include <score/comm/ShmSampleBuffer.hpp>
#include <score/middleware/Runnable.hpp>
#include <queue.hpp>

#include <runnable_interfaces.hpp>  // Common interface header for Runnable communication

struct PerformanceTracerHiddenState {
};

class PerformanceTracer final : public score::middleware::Runnable<PerformanceTracerInputs,
                                                                            PerformanceTracerOutputs,
                                                                            PerformanceTracerInternalState,
                                                                            PerformanceTracerParameters> {
  public:
    std::error onInit(const PerformanceTracerParameters& parameters,
                            PerformanceTracerInternalState& internal_state) noexcept final;
    std::error onUpdate(const std::chrono::time_point& time,
                              const PerformanceTracerParameters& parameters,
                              const PerformanceTracerInputs& inputs,
                              PerformanceTracerInternalState& internal_state,
                              PerformanceTracerOutputs& outputs) noexcept final;
    std::error onShutdown(const PerformanceTracerParameters& parameters,
                                PerformanceTracerInternalState& internal_state) noexcept final;
    std::error onReset(const PerformanceTracerParameters& parameters,
                             PerformanceTracerInternalState& internal_state) noexcept;
  private:
    PerformanceTracerHiddenState hidden_state_;
};

#endif  // PERFORMANCETRACER_HPP
