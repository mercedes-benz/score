// This file was generated by the ScoreGen C++ template for runnable implementation skeletons

#include "runnable.hpp"  // Needs to be included first

#include <system_error>
#include <score/log/Logger.hpp>

static constexpr std::string_view kLogPrefix{ "PerformanceTracer" };

std::error PerformanceTracer::onInit(const PerformanceTracerParameters& parameters,
                                                      PerformanceTracerInternalState& internal_state) noexcept {
    return std::error{std::errorCode::kSuccess};
}

std::error PerformanceTracer::onUpdate(const std::chrono::time_point& time,
                                                        const PerformanceTracerParameters& parameters,
                                                        const PerformanceTracerInputs& inputs,
                                                        PerformanceTracerInternalState& internal_state,
                                                        PerformanceTracerOutputs& outputs) noexcept {
    if (!inputs.camera_perf_trace_input_queue.empty()) {
        const adas::perception::CameraFrame& camera_perf_trace = *inputs.camera_perf_trace_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }
    if (!inputs.neural_net_perf_trace_input_queue.empty()) {
        const adas::perception::ObjectDetections& neural_net_perf_trace = *inputs.neural_net_perf_trace_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }
    if (!inputs.brake_perf_trace_input_queue.empty()) {
        const adas::perception::BrakeCommand& brake_perf_trace = *inputs.brake_perf_trace_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }
    if (!inputs.traffic_sign_perf_trace_input_queue.empty()) {
        const adas::perception::TrafficSignCommand& traffic_sign_perf_trace = *inputs.traffic_sign_perf_trace_input_queue.front();
        // TODO(end-user): Write your input processing code here
    }

    std::error output_result{std::errorCode::kSuccess};
    // The below output access examples are for reference only
    output_result = outputs.performance_traces_output_queue.acquire().with_value([&parameters](auto& output_) {
        ; // TODO(end-user): Write something to the output, e.g. output_->my_field = 0;
        score::base::ignore = parameters;
        score::base::ignore = output_;
    }).error_or(std::error{std::errorCode::kSuccess});

    return output_result;
}

std::error PerformanceTracer::onReset(const PerformanceTracerParameters& parameters,
                                                       PerformanceTracerInternalState& internal_state) noexcept {
    return std::error{std::errorCode::kSuccess};
}

std::error PerformanceTracer::onShutdown(const PerformanceTracerParameters& parameters,
                                                          PerformanceTracerInternalState& internal_state) noexcept {
    return std::error{std::errorCode::kSuccess};
}
